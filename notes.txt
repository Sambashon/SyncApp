
$sqlUser = "INSERT INTO users(username, email, passHash) 
            VALUES ('$username', '$email', '$passHash')"; #this is what actually inserts stuff in the DB
THIS IS UNSAFE, it is vulnerable to sql inyections.
if($conn->query($sqlUser) === TRUE){#Checks if whether it failed or not "->" accesses properties or methods of an object like the "." in js
    echo "User registered succesfully";
}else{
    echo "Failed to register" . $conn->error;
}
